def posterior():

    # Needs max and min values for parameters of age, metallicity, mass,
    # and rotation in order to calc priors. Need to go data point by data
    # point. I could do this for one point and loop through and multiply
    # the results of each calculation to get the joint posterior for the set.
    
    return
